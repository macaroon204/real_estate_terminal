# 🏗️ Front (Vite + React + TypeScript)

> 부동산 **지가 시각화 대시보드** 프론트엔드\
> Node **20.19.5** / npm **10.8.2**\
> 빌드: **Vite + React 19 + TypeScript**

------------------------------------------------------------------------

# ⚙️ 0. 기술 스택

  영역                 사용 기술
  -------------------- -------------------------------------
  **프레임워크**       React 19
  **번들러**           Vite
  **언어**             TypeScript
  **라우터**           React Router v7
  **스타일링**         CSS (폴더 단위 분리)
  **환경 관리**        NVM for Windows
  **품질 도구**        ESLint · Prettier
  **Node 버전 관리**   `.nvmrc` + PowerShell Trim 스크립트

------------------------------------------------------------------------

# 📦 1. 개발 환경 요구사항

  항목                  버전                        비고
  --------------------- --------------------------- ----------------
  **Node.js**           20.19.5                     팀 공통 버전
  **npm**               10.8.2                      패키지
  **NVM for Windows**   1.2.2+                      Node 버전 관리
  **권장 터미널**       PowerShell / CMD / VSCode   모두 동일 동작

------------------------------------------------------------------------

# 🚀 2. 프로젝트 세팅

## 0️⃣ Node 버전 적용 (Windows 환경 안정 방법)

``` bash
npm run node
```

Windows의 NVM은 `.nvmrc`를 자동으로 읽지 않거나,\
CRLF/LF, 줄 끝 공백 등으로 인해 버전 인식이 실패할 수 있다.

본 프로젝트는 다음 방식으로 **안전하게 동작하도록 처리**한다:

``` powershell
(Get-Content .nvmrc).Trim()
```

→ 공백, 개행 문제 제거\
→ PowerShell/CMD/VSCode 모두에서 동일하게 Node 버전 세팅됨.

------------------------------------------------------------------------

## 1️⃣ 의존성 설치

``` bash
npm ci
```

------------------------------------------------------------------------

## 2️⃣ 개발 서버 실행

``` bash
npm run dev
```

http://localhost:5173

------------------------------------------------------------------------

## 3️⃣ 코드 검사/포맷

``` bash
npm run lint
npm run lint:fix
npm run format
```

------------------------------------------------------------------------

# 🧠 3. package.json 스크립트 설명

``` json
"scripts": {
  "node": "powershell -NoProfile -ExecutionPolicy Bypass -Command \"$v = (Get-Content .nvmrc).Trim(); nvm use $v\"",
  "dev": "vite",
  "build": "tsc -b && vite build",
  "preview": "vite preview",
  "lint": "eslint .",
  "lint:fix": "eslint . --fix",
  "format": "prettier --write \"**/*.{ts,tsx,js,json,css,md}\""
}
```

  명령           설명
  -------------- -------------------------------------------------------
  **node**       `.nvmrc` 버전 Trim 후 `nvm use` 실행 (Windows 안정화)
  **dev**        개발 서버 실행
  **build**      TS 빌드 + Vite 빌드
  **preview**    빌드 결과 로컬 확인
  **lint**       ESLint 검사
  **lint:fix**   에러 자동 수정
  **format**     Prettier 포맷 일괄 적용

------------------------------------------------------------------------

# 🧩 4. 전체 동작 흐름 (Pages Driven)

### 페이지 전환 흐름

    index.html
      ↓
    main.tsx
      ↓
    App.tsx  ← RouterProvider
      ↓
    Routes
      ├─ "/"     → HomePage
      └─ "/main" → MainPage

### MainPage 내부 레이아웃

    PageFrame
     ├─ Top        ← 로고 + 검색
     ├─ LeftPanel  ← 지역 선택 아코디언
     └─ Center     ← 그래프·지표 영역 (Outlet 포함)

------------------------------------------------------------------------

# 🧭 5. 라우터 구조

``` tsx
createBrowserRouter([
  { path: '/', Component: HomePage },
  { path: '/main', Component: MainPage }
]);
```

MainPage의 `<Outlet />` 때문에 다음과 같은 확장이 바로 가능:

    /main/nationwide
    /main/metro/:code
    /main/metro/:code/:gu

------------------------------------------------------------------------

# 📊 6. 좌측 패널 구조 요약 (아코디언)

    <ul>
      ├─ [광역 헤더]
      │     └─ 토글 버튼
      ├─ [하위지역 바디] x N
      ├─ [광역 헤더]
      └─ ...

-   openSet 기반 상태 관리\
-   헤더 클릭 → openSet toggle → 바디 DOM 표시/삭제\
-   전국 1줄 / 광역 헤더 / 하위지역 바디 구조 분리

------------------------------------------------------------------------

# 📁 7. 폴더 구조 (단조로운 최신 버전)

``` txt
src/
 ├─ pages/
 │   ├─ data/
 │   ├─ HomePage.tsx
 │   └─ MainPage.tsx
 │
 ├─ main/
 │   ├─ global/
 │   ├─ left/
 │   └─ top/
 │
 ├─ App.tsx
 └─ main.tsx
```

------------------------------------------------------------------------

# ⚡ 8. 팀원용 전체 실행 요약

``` bash
1) npm run node    # Node 버전 자동 적용
2) npm ci          # 의존성 설치
3) npm run dev     # 개발 서버 실행
```

------------------------------------------------------------------------
